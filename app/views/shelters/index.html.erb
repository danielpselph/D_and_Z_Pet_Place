<!DOCTYPE html>
<html>
<head>
<style>
.deets {
  background-color: lightblue;
  border-style: outset;
  padding: 10px;
  width: 17.5%;
}
</style>
</head>
<body>

<h1>All Shelters</h1>

<% @shelters.each do |shelter| %>
  <div class="deets">
  <section id="shelter-<%= shelter.id %>">
    <h4>Name: <%= link_to "#{shelter.name}", "/shelters/#{shelter.id}" %></h4>
    <p>
    <p>
      <%= button_to 'Show', "/shelters/#{shelter.id}", method: :get %>
      <%= button_to 'Edit', "/shelters/#{shelter.id}/edit", method: :get %>
      <% if shelter.has_pending_pets? == false %>
        <%= button_to "Delete", "/shelters/#{shelter.id}", method: :destroy %>
      <% end %>
    </p>
  </section>
  </div>
  <br>
<% end %>
<%= button_to "New Shelter", '/shelters/new', method: :get %>
